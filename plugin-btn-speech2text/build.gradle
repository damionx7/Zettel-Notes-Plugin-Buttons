def versionMajor = 8

android {
    defaultConfig {
        applicationId "org.eu.thedoc.zettelnotes.buttons.speech2text"
        versionCode versionMajor
        versionName "${versionMajor}"
    }
}